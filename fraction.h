//
class  Fraction{
	void simple();//程虏だ计 
	int gcd(int a,int b); //程そ计 
	public:
		int sub;	//だ 
		int base;	//だダ 
		Fraction(int,int);//篶(だ,だダ) 
		Fraction operator+(Fraction); 
		Fraction operator-(Fraction); 
		Fraction operator*(Fraction); 
		Fraction operator/(Fraction);
};

#include"fraction.cpp"


